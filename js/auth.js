async function hash(text){const msg=new TextEncoder().encode(text);const buf=await crypto.subtle.digest("SHA-256",msg);return Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,"0")).join("");}
async function register(){let u=document.getElementById("user").value,p=document.getElementById("pass").value;if(!u||!p){alert("Fill both fields");return;}let users=JSON.parse(localStorage.getItem("users")||"{}");if(users[u]){alert("User exists");return;}users[u]=await hash(p);localStorage.setItem("users",JSON.stringify(users));alert("Account created");}
async function login(){let u=document.getElementById("user").value,p=document.getElementById("pass").value,users=JSON.parse(localStorage.getItem("users")||"{}");if(users[u]===await hash(p)){localStorage.setItem("loggedUser",u);window.location="dashboard.html";}else{alert("Wrong login");}}
function toggleTheme(){let theme=document.body.classList.contains("light")?"dark":"light";document.body.classList.remove("light","dark");document.body.classList.add(theme);localStorage.setItem("theme",theme);}
